spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/dormitory
  data:
    redis:
      host: redis
  kafka:
    bootstrap-servers: kafka:9092

management:
  otlp:
    tracing:
      endpoint: http://tempo:4318/v1/traces
