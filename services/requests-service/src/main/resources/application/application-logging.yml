logging:
  level:
    root: INFO
    ru.dstu.dormitory.requests_service: DEBUG
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]"
