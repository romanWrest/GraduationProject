databaseChangeLog:
  - changeSet:
      id: 002-create-outbox-events
      author: requests-service
      changes:
        - createTable:
            schemaName: requests
            tableName: outbox_events
            columns:
              - column:
                  name: id
                  type: UUID
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: aggregate_type
                  type: VARCHAR(64)
                  constraints:
                    nullable: false
              - column:
                  name: aggregate_id
                  type: VARCHAR(128)
                  constraints:
                    nullable: false
              - column:
                  name: event_type
                  type: VARCHAR(64)
                  constraints:
                    nullable: false
              - column:
                  name: payload
                  type: JSONB
                  constraints:
                    nullable: false
              - column:
                  name: created_at
                  type: TIMESTAMP WITH TIME ZONE
                  defaultValueComputed: NOW()
                  constraints:
                    nullable: false
              - column:
                  name: sent_at
                  type: TIMESTAMP WITH TIME ZONE

  - changeSet:
      id: 002-outbox-indexes
      author: requests-service
      changes:
        - sql:
            sql: "CREATE INDEX idx_outbox_unsent ON requests.outbox_events (created_at) WHERE sent_at IS NULL"
            dbms: postgresql
