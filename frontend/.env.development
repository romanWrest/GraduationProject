# Пусто → axios идёт по same-origin, /api/v1 проксируется через Vite dev-server
# на http://localhost:8080 (см. vite.config.ts → server.proxy).
# Это обходит дубли CORS-заголовков, которые вешает gateway+auth.
VITE_API_BASE_URL=
VITE_NOTIFICATIONS_POLL_INTERVAL_MS=30000
